<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="workshop.dao.CustomerMapper">

	<resultMap type="customer" id="customerMap">
		<id column="cust_id" property="custId"/>
		<result column="cust_name" property="custName"/>
		<result column="pass" property="custPass"/>
	</resultMap>

	<select id="selectAllCustomer" resultMap="customerMap">
		select * from customer
	</select>
	
	<select id="selectCustomerById" parameterType="String" resultMap="customerMap">
		select * from customer where cust_id = #{custNo}
	</select>
	
	<delete id="deleteCustomerById" parameterType="String">
		delete from customer where cust_id = #{custNo}
	</delete>
	
	<update id="updateCustomer" parameterType="customer">
		update customer set cust_name = #{custName}, pass = #{custPass}
		where cust_id = #{custId}
	</update>
	
	<insert id="insertCustomer" parameterType="customer">
		insert into customer (cust_id, cust_name, pass)
		values(#{custId}, #{custName}, #{custPass})
	</insert>
	
</mapper>