<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.dao.CommentMapper">
	
	<insert id="insert" parameterType="comment">
		<selectKey keyProperty="commentNo" resultType="int" order="AFTER">
			select SEQ_COMMENT.currval as commentNo from dual
		</selectKey>
		insert into tcomment (user_id, content) values ( #{userId}, #{content}) 
	</insert>
	
	<select id="selectAllComment" resultType="comment">
		select *
		from tcomment
		order by comment_no
	</select>
	
</mapper>